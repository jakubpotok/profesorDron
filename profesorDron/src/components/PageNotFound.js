/** @format */

import React from 'react';

const PageNotFound = () => {
  const styles = {
    paddingBottom: '28rem',
  };

  return <div style={styles}>Page Not Found</div>;
};

export default PageNotFound;
